{
  "name": "loancalc",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "node build.js",
    "deploy": "node build.js && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "esbuild": "^0.25.0"
  }
}
